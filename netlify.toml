[build]
  publish = "dist" # Diretório onde os arquivos de build são gerados
  command = "npm run build" # Comando para construir o projeto

[[redirects]]
  from = "/*"
  to = "/homepage.html"
  status = 200
